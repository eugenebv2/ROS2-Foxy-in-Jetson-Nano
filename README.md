# ROS2-Foxy-in-Jetson-Nano

# Jetson nano install Ros2 Foxy

test ROS2 Control need

sudo apt install ros-foxy-ros2-control ros-foxy-ros2-controllers ros-foxy-gazebo-ros2-control

# Using dicker exec ROS2 humble

